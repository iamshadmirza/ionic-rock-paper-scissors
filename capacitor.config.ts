import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-rock-paper-scissors',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
